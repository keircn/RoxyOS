# Maintainer: Keiran <keircn@proton.me>

pkgname=roxyos-ghostty
pkgver=1.0.0
pkgrel=1
pkgdesc="Roxy Migurdia themed Ghostty terminal configuration"
arch=('any')
url="https://github.com/keircn/RoxyOS"
license=('MIT')
depends=('ghostty')
options=('!strip')

package() {
    cp -r "${startdir}/usr" "${pkgdir}/"
}
