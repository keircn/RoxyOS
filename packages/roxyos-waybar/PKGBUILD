# Maintainer: Keiran <keircn@proton.me>

pkgname=roxyos-waybar
pkgver=1.0.2
pkgrel=1
pkgdesc="Roxy Migurdia themed Waybar configuration"
arch=('any')
url="https://github.com/keircn/RoxyOS"
license=('MIT')
depends=('waybar')
options=('!strip')

package() {
    cp -r "${startdir}/usr" "${pkgdir}/"
}
