# Maintainer: Keiran <keircn@proton.me>

pkgname=roxyos-kitty
pkgver=1.0.5
pkgrel=1
pkgdesc="Roxy Migurdia themed Kitty terminal configuration"
arch=('any')
url="https://github.com/keircn/RoxyOS"
license=('MIT')
depends=('kitty')
options=('!strip')

package() {
    cp -r "${startdir}/usr" "${pkgdir}/"
}
